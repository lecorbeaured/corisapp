# ═══════════════════════════════════════
# CORIS Production Environment
# Copy to .env and fill in real values
# ═══════════════════════════════════════

# ── Database ──
DATABASE_URL=postgres://coris:YOUR_STRONG_PASSWORD@db:5432/coris
DB_PASSWORD=YOUR_STRONG_PASSWORD

# ── Server ──
PORT=3000
NODE_ENV=production

# ── Auth ──
# Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=CHANGE_ME_TO_RANDOM_64_CHAR_STRING

# ── Cookies ──
# Set to your domain (e.g. coris.app) — no protocol, no port
COOKIE_DOMAIN=
# Must be true in production (requires HTTPS)
COOKIE_SECURE=true
CSRF_ENABLED=true

# ── CORS ──
# Your frontend URL (e.g. https://coris.app)
# Not needed if frontend and API are on the same origin (Nginx proxy)
CORS_ALLOWED_ORIGINS=
CORS_ALLOW_ALL=false

# ── App URL (for password reset emails) ──
APP_PUBLIC_URL=https://YOUR_DOMAIN

# ── Email (SMTP) ──
# Required for password reset and due-today reminders
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-smtp-password
SMTP_FROM="CORIS <no-reply@YOUR_DOMAIN>"

# ── DB Functions (usually don't change) ──
DB_FN_GENERATE_WINDOWS_FOR_SCHEDULE=coris_generate_paycheck_windows_for_schedule
DB_FN_ASSIGN_OCCURRENCES_TO_ACTIVE_WINDOWS=coris_assign_occurrences_to_active_windows
DB_FN_REFRESH_PLANNING_FOR_USER=
DB_FN_GENERATE_OCCURRENCES_FOR_USER=coris_generate_bill_occurrences_for_user

# ── Reminders ──
REMINDER_BATCH_SIZE=50
